name: Legacy Script Execution Test (manual)

on:
  workflow_dispatch:

jobs:
  archived:
    runs-on: ubuntu-latest
    steps:
      - name: Legacy workflow notice
        run: |
          echo "This legacy workflow is archived."
          echo "Use .github/workflows/ci.yml for active CI/CD validation."
